# VPanel - 企业级服务器运维面板

<div align="center">
  <h1>🚀 VPanel</h1>
  <p><strong>新一代智能服务器运维管理平台</strong></p>
  <p>vStats 的究极进化版 - 全功能服务器运维解决方案</p>
</div>

---

## ✨ 核心特性

### 🔌 插件化架构
- 动态插件加载/卸载
- 官方插件市场
- 自定义插件开发 SDK
- 插件依赖管理

### 🐳 Docker 管理
- 容器全生命周期管理
- Docker Compose 编排
- 镜像管理与构建
- 容器日志与终端
- 资源限制与监控

### 🌐 Nginx 管理
- 站点配置可视化
- SSL 证书自动申请 (Let's Encrypt)
- 反向代理配置
- 负载均衡管理
- 实时访问日志分析

### 🗄️ 数据库管理
- MySQL/MariaDB 管理
- PostgreSQL 管理
- Redis/MongoDB 支持
- 备份与恢复
- 性能监控

### 📦 软件管理
- 一键安装/卸载
- 版本管理与升级
- 软件商店
- 自定义软件源

### 📊 系统监控
- 实时性能监控
- 历史数据分析
- 告警通知
- 多节点管理

### 🔐 安全管理
- 防火墙管理
- Fail2Ban 集成
- SSH 密钥管理
- 安全审计日志

### 📁 文件管理
- Web 文件管理器
- 在线代码编辑
- 权限管理
- 压缩/解压

### 💻 终端管理
- Web SSH 终端
- 多会话支持
- 命令历史
- 协作终端

### ⏰ 计划任务
- Cron 可视化管理
- 任务日志
- 执行历史
- 任务模板

---

## 🏗️ 技术架构

```
┌─────────────────────────────────────────────────────────────────────┐
│                         VPanel Architecture                         │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                     Frontend (Web UI)                        │   │
│  │  React 18 + TypeScript + Vite + TailwindCSS + Zustand       │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                              │                                       │
│                              ▼                                       │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                    API Gateway Layer                         │   │
│  │           RESTful API + WebSocket + gRPC                     │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                              │                                       │
│         ┌────────────────────┼────────────────────┐                 │
│         ▼                    ▼                    ▼                 │
│  ┌────────────┐      ┌────────────┐      ┌────────────┐            │
│  │   Core     │      │  Plugins   │      │   Agent    │            │
│  │  Services  │◄────►│   System   │◄────►│  Manager   │            │
│  └────────────┘      └────────────┘      └────────────┘            │
│         │                    │                    │                 │
│         ▼                    ▼                    ▼                 │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                     Storage Layer                            │   │
│  │        SQLite/PostgreSQL + Redis + File System              │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘

                    Remote Agents (Multiple Nodes)
┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐
│   Agent 1   │  │   Agent 2   │  │   Agent 3   │  │   Agent N   │
│  (Node A)   │  │  (Node B)   │  │  (Node C)   │  │  (Node N)   │
└──────┬──────┘  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘
       │                │                │                │
       └────────────────┴────────────────┴────────────────┘
                              │
                              ▼
                    WebSocket / gRPC Connection
```

---

## 📁 项目结构

```
vpanel/
├── server/                    # 后端服务 (Go)
│   ├── cmd/
│   │   ├── server/           # 主服务入口
│   │   └── migrate/          # 数据库迁移工具
│   ├── internal/
│   │   ├── config/           # 配置管理
│   │   ├── database/         # 数据库层
│   │   ├── models/           # 数据模型
│   │   ├── services/         # 业务服务层
│   │   │   ├── auth/         # 认证服务
│   │   │   ├── docker/       # Docker 管理
│   │   │   ├── nginx/        # Nginx 管理
│   │   │   ├── database/     # 数据库管理
│   │   │   ├── file/         # 文件管理
│   │   │   ├── terminal/     # 终端服务
│   │   │   ├── cron/         # 计划任务
│   │   │   ├── firewall/     # 防火墙管理
│   │   │   └── monitor/      # 监控服务
│   │   ├── handlers/         # HTTP 处理器
│   │   ├── middleware/       # 中间件
│   │   ├── plugin/           # 插件系统核心
│   │   │   ├── registry/     # 插件注册中心
│   │   │   ├── loader/       # 插件加载器
│   │   │   ├── sandbox/      # 插件沙箱
│   │   │   └── api/          # 插件 API
│   │   └── utils/            # 工具函数
│   ├── pkg/                  # 公共包
│   │   ├── logger/           # 日志
│   │   ├── response/         # 统一响应
│   │   ├── validator/        # 验证器
│   │   └── crypto/           # 加密工具
│   ├── plugins/              # 官方插件
│   │   ├── php/              # PHP 环境插件
│   │   ├── nodejs/           # Node.js 环境插件
│   │   ├── python/           # Python 环境插件
│   │   ├── java/             # Java 环境插件
│   │   └── wordpress/        # WordPress 插件
│   ├── go.mod
│   └── go.sum
│
├── agent/                     # 远程代理 (Go)
│   ├── cmd/
│   │   └── agent/            # Agent 入口
│   ├── internal/
│   │   ├── collector/        # 指标收集
│   │   ├── executor/         # 命令执行器
│   │   ├── tunnel/           # 隧道服务
│   │   └── updater/          # 自动更新
│   ├── go.mod
│   └── go.sum
│
├── web/                       # 前端应用 (React)
│   ├── src/
│   │   ├── api/              # API 客户端
│   │   ├── components/       # 通用组件
│   │   │   ├── ui/           # UI 基础组件
│   │   │   ├── layout/       # 布局组件
│   │   │   └── business/     # 业务组件
│   │   ├── pages/            # 页面
│   │   │   ├── dashboard/    # 仪表盘
│   │   │   ├── docker/       # Docker 管理
│   │   │   ├── nginx/        # Nginx 管理
│   │   │   ├── database/     # 数据库管理
│   │   │   ├── files/        # 文件管理
│   │   │   ├── terminal/     # 终端
│   │   │   ├── cron/         # 计划任务
│   │   │   ├── firewall/     # 防火墙
│   │   │   ├── settings/     # 系统设置
│   │   │   ├── plugins/      # 插件管理
│   │   │   └── logs/         # 日志中心
│   │   ├── stores/           # 状态管理 (Zustand)
│   │   ├── hooks/            # 自定义 Hooks
│   │   ├── utils/            # 工具函数
│   │   ├── types/            # TypeScript 类型
│   │   ├── i18n/             # 国际化
│   │   └── styles/           # 样式
│   ├── public/
│   ├── package.json
│   └── vite.config.ts
│
├── sdk/                       # 插件开发 SDK
│   ├── go/                   # Go SDK
│   └── docs/                 # SDK 文档
│
├── deploy/                    # 部署配置
│   ├── docker/
│   │   ├── Dockerfile
│   │   ├── Dockerfile.agent
│   │   └── docker-compose.yml
│   ├── systemd/
│   └── scripts/
│
├── docs/                      # 文档
│   ├── api/                  # API 文档
│   ├── plugin-dev/           # 插件开发指南
│   └── deployment/           # 部署文档
│
└── Makefile
```

---

## 🚀 快速开始

### 一键安装
```bash
curl -sSL https://get.vpanel.io | bash
```

### Docker 安装
```bash
docker run -d \
  --name vpanel \
  -p 8080:8080 \
  -v /var/run/docker.sock:/var/run/docker.sock \
  -v vpanel-data:/data \
  vpanel/vpanel:latest
```

### 开发环境
```bash
# 克隆仓库
git clone https://github.com/your-org/vpanel.git
cd vpanel

# 启动后端
cd server && go run ./cmd/server

# 启动前端 (新终端)
cd web && npm install && npm run dev
```

---

## 📦 插件开发

VPanel 提供强大的插件系统，允许开发者扩展面板功能。

### 插件类型
1. **服务插件** - 安装和管理特定服务
2. **工具插件** - 提供额外的管理工具
3. **监控插件** - 扩展监控能力
4. **主题插件** - 自定义 UI 主题

### 开发示例
```go
package main

import (
    "github.com/vpanel/sdk-go/plugin"
)

func main() {
    p := plugin.New(&plugin.Config{
        ID:          "my-plugin",
        Name:        "My Plugin",
        Version:     "1.0.0",
        Description: "A custom plugin for VPanel",
    })

    // 注册 API 路由
    p.RegisterRoute("GET", "/status", handleStatus)
    
    // 注册菜单项
    p.RegisterMenu(&plugin.MenuItem{
        Title: "My Plugin",
        Icon:  "plugin",
        Path:  "/plugins/my-plugin",
    })

    p.Run()
}
```

---

## 🔒 安全特性

- **多因素认证 (MFA)** - TOTP 支持
- **RBAC 权限控制** - 细粒度权限管理
- **API 密钥管理** - 安全的 API 访问
- **审计日志** - 完整的操作记录
- **加密存储** - 敏感数据加密
- **自动安全更新** - 漏洞自动修复

---

## 📄 License

Apache License 2.0

---

<div align="center">
  <p>Made with ❤️ by VPanel Team</p>
</div>

# vpanel
